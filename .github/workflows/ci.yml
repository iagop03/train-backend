name: CI/CD Backend

on:
  push:
    branches: [ main, develop ]
  pull_request:
    branches: [ main, develop ]

jobs:
  build:
    runs-on: ubuntu-latest
    
    services:
      postgres:
        image: postgres:15
        env:
          POSTGRES_PASSWORD: postgres
        options: >-
          --health-cmd pg_isready
          --health-interval 10s
          --health-timeout 5s
          --health-retries 5
        ports:
          - 5432:5432
    
    steps:
    - uses: actions/checkout@v4
    
    - name: Set up JDK 21
      uses: actions/setup-java@v4
      with:
        java-version: '21'
        distribution: 'temurin'
        cache: maven
    
    - name: Build with Maven
      run: mvn clean package -DskipTests
    
    - name: Run Tests
      run: mvn test
      env:
        SPRING_DATASOURCE_URL: jdbc:postgresql://localhost:5432/train_test
        SPRING_DATASOURCE_USERNAME: postgres
        SPRING_DATASOURCE_PASSWORD: postgres
    
    - name: SonarQube Analysis
      run: mvn sonar:sonar -Dsonar.projectKey=train-backend
      env:
        SONAR_HOST_URL: ${{ secrets.SONAR_HOST_URL }}
        SONAR_LOGIN: ${{ secrets.SONAR_LOGIN }}
      continue-on-error: true
    
    - name: Upload to GCP Artifact Registry
      if: github.ref == 'refs/heads/main'
      run: |
        echo ${{ secrets.GCP_SA_KEY }} | base64 -d > /tmp/key.json
        gcloud auth activate-service-account --key-file=/tmp/key.json
        gcloud builds submit --tag gcr.io/${{ secrets.GCP_PROJECT }}/train-backend:${{ github.sha }}
